---
features:
  - |
    Adds ``ops-tools/default_password_detector/default_password_detector.py``,
    a read-only scanner that renders enabled OpenStack Helm charts at their
    pinned versions and reports live Kubernetes secret values that still match
    sensitive chart defaults. The tool supports text and JSON reports and does
    not disclose secret values.
