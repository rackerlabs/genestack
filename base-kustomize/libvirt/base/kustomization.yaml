---
resources:
  - all.yaml

patches:
  - target:
      kind: DaemonSet
      name: libvirt-libvirt-default
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/1/args
        value:
         - --libvirt.uri=qemu+unix:///system?socket=/run/libvirt/libvirt-sock-ro
         - --web.listen-address=:9474
