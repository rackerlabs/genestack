---
- name: Write MetalLB configuration
  ansible.builtin.template:
    src: metallb-openstack-service-lb.yml.j2
    dest: "{{ metallb_manifest_path }}"
    mode: "0644"
  when: metal_lb_ip | length > 0
