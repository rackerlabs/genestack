bases:
  - "../base"

patches:
  - target:
      kind: MariaDB
      name: mariadb-cluster
    patch: |-
      - op: replace
        path: /spec/affinity/enableAntiAffinity
        value: false
      - op: replace
        path: /spec/replicas
        value: 2
