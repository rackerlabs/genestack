apiVersion: batch/v1
kind: CronJob
metadata:
  name: ops-tools-ovn-find-stale-ip-crd
  namespace: openstack
  labels:
    app.kubernetes.io/name: ops-tools
    app.kubernetes.io/component: ovn-find-stale-ip-crd
    app.kubernetes.io/part-of: genestack
spec:
  schedule: "13 */6 * * *"
  suspend: true
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 7
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 604800
      backoffLimit: 0
      activeDeadlineSeconds: 1800
      template:
        metadata:
          labels:
            app.kubernetes.io/name: ops-tools
            app.kubernetes.io/component: ovn-find-stale-ip-crd
            app.kubernetes.io/part-of: genestack
        spec:
          serviceAccountName: ops-tools
          restartPolicy: Never
          containers:
            - name: find-ovn-stale-ip-crd
              image: ghcr.io/rackerlabs/genestack-images/ops-tools:latest
              imagePullPolicy: IfNotPresent
              command:
                - /bin/bash
                - -lc
              args:
                - |
                  set -uo pipefail
                  rc=0
                  python /opt/genestack/ops-tools/ovn/find_ovn_stale_ip_crd.py --format json --quiet || rc=$?
                  if [ "${rc}" -eq 2 ]; then
                    exit 0
                  fi
                  exit "${rc}"
              terminationMessagePolicy: FallbackToLogsOnError
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
