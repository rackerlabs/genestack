apiVersion: batch/v1
kind: CronJob
metadata:
  name: ops-tools-find-orphan-qdisk
  namespace: openstack
  labels:
    app.kubernetes.io/name: ops-tools
    app.kubernetes.io/component: find-orphan-qdisk
    app.kubernetes.io/part-of: genestack
spec:
  schedule: "17 */6 * * *"
  suspend: true
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 7
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 604800
      backoffLimit: 0
      activeDeadlineSeconds: 1800
      template:
        metadata:
          labels:
            app.kubernetes.io/name: ops-tools
            app.kubernetes.io/component: find-orphan-qdisk
            app.kubernetes.io/part-of: genestack
        spec:
          serviceAccountName: ops-tools
          restartPolicy: Never
          containers:
            - name: find-orphan-qdisk
              image: ghcr.io/rackerlabs/genestack-images/ops-tools:latest
              imagePullPolicy: IfNotPresent
              command:
                - /bin/bash
                - -lc
              args:
                - |
                  set -uo pipefail
                  rc=0
                  python /opt/genestack/ops-tools/find_orphan_qdisk/find_orphan_qdisk.py --format json || rc=$?
                  if [ "${rc}" -eq 2 ]; then
                    exit 0
                  fi
                  exit "${rc}"
              terminationMessagePolicy: FallbackToLogsOnError
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
